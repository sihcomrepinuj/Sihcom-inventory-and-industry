flask>=3.0
gunicorn>=21.2
preston>=4.12
requests>=2.31
pytest>=8.0

# eve-sde-converter (run as a subprocess by setup_sde.py) needs these.
# The converter's own requirements.txt lists many more libraries (numpy,
# openpyxl, psycopg2-binary, pymssql, etc.) but those are for unused
# export scripts — the tableloader code that runs during SQLite
# conversion only imports sqlalchemy, yaml, and requests.
sqlalchemy>=2.0
pyyaml>=6.0
